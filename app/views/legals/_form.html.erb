<%= form_for([@legal.project, @legal]) do |f| %>
  <% if @legal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@legal.errors.count, "error") %> prohibited this legal from being saved:</h2>

      <ul>
      <% @legal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
